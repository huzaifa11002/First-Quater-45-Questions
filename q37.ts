/*Large Shirts: Modify the make_shirt() function so that shirts are large by default with a message that reads I love TypeScript. 
Make a large shirt and a medium shirt with the default message, and a shirt of any size with a different message. */

//Large T-shirt Size Function
// function make_shirt( message: string, size: string = "Large"): void {
//     console.log(`The size of the shirt is ${size} and the message is ${message}`);
// }

// make_shirt(" I love TypeScript");

//Medium T-shirt Size Function
// function make_shirt( message: string, size: string = "Medium"): void {
//     console.log(`The size of the shirt is ${size} and the message is ${message}`);
// }

// make_shirt(" I love TypeScript");

//Any T-shirt Size Function
// function make_shirt( message: string, size: string = "Any Size"): void {
//     console.log(`The size of the shirt is ${size} and the message is ${message}`);
// }

// make_shirt(" I love Python");